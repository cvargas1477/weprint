<?php

use App\Cliente;
use App\User;
use Illuminate\Database\Seeder;

class ClienteSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        //$users = User::all();
        //$cliente = factory(Cliente::class, 35)->make();

        //$cliente->each(function($cliente) use ($users){
         //   $users = $users->random();
         //   $cliente->users_id = $users->id;
         //   $cliente->save();

        //});

    }
}
